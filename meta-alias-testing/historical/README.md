# Historical records

Superseded notes kept for reference. They document the first engine approach and
an earlier (shadowing-based) resolution model that was later corrected to
[../meta-loading.md](../meta-loading.md). Fresh baseline/status will be recorded
against the reworked implementation.

- `BASELINE.md` — what the unmodified engine did (still accurate as history; its
  "after fix" expectations use the old model).
- `STATUS.md` — the change-by-change engine experiments (index / fallback), before
  the override-identity rework.
